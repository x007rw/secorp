# このファイルを `.env` にコピーして使用してください
# .env はGit追跡対象外（.gitignore で除外）

# WordPress / phpMyAdmin の公開ポート
WP_PORT=8080
PMA_PORT=8081

# MySQL 設定
MYSQL_DATABASE=secorp_wp
MYSQL_USER=wp_user
MYSQL_PASSWORD=wp_password
MYSQL_ROOT_PASSWORD=root_password
